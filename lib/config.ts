// Hardcoded reference date for all phase-progression math.
// The demo data is deterministic regardless of when the reviewer views it.
// NEVER use Date.now() / new Date() for "days deployed" / "days in phase".
export const REFERENCE_DATE = "2026-06-02";
