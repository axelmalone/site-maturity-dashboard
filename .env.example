# OpenAI API key — used server-side by /api/chat for the agent layer.
# The browser never sees this key. The dashboard renders without it;
# only the chat agent requires it.
OPENAI_API_KEY=your-key-here
